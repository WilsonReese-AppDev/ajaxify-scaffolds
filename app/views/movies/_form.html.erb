<%= form_with(model: movie, local: false) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= render "movies/form_title", form: form %>
  </div>

  <div class="field">
    <%= render "movies/form_year", form: form %>
  </div>

  <div class="field">
    <%= render "movies/form_director", form: form %>
  </div>

  <div class="field">
    <%= render "movies/form_description", form: form %>
  </div>

  <div class="field">
    <%= render "movies/form_duration", form: form %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
